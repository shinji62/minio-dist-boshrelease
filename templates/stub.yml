---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - minio_dist
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: minio-dist
    version: latest
